box: ubuntu

build:
  steps:
    - script:
      name: install dependencies
      code: |
        sudo apt-get update
        sudo apt install libeigen3-dev libpcl-dev cmake
        Y
    - script:
      name: build
      code: |
        mkdir build
        cd build
        cmake ..
        make -j4


